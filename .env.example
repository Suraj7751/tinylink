\
    # .env.example
    # Replace with your Neon/PG connection string
    DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

    # Set this to your deployed domain (used for copy links in UI)
    NEXT_PUBLIC_BASE_URL="https://your-domain.vercel.app"

    # Local path to the assignment pdf (include in repo/README if desired)
    ASSIGNMENT_PDF_PATH="/mnt/data/Take-Home Assignment_ TinyLink (1) (2).pdf"
